{
  "_comment": "ARC config template — copy to the location shown by `arc auth show`",
  "_note": "Secrets (bearer_token, client_secret, ssh.password) are stored in the OS keychain or temporary env vars, not here",
  "scm": {
    "_note": "Use bearer_token OR OAuth. Leave bearer_token and client_secret blank in this file; enter secrets with arc auth login.",
    "bearer_token": "",
    "client_id": "your-scm-client-id",
    "client_secret": "",
    "tsg_id": "your-scm-tsg-id"
  },
  "ssh": {
    "_note": "SSH credentials — prefer key_path over password",
    "user": "admin",
    "key_path": "~/.ssh/panos_key",
    "password": "",
    "port": 22
  },
  "default_folder": "Shared"
}
